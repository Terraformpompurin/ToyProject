# ── 웹 프레임워크 ──────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0       # ASGI 서버
python-multipart>=0.0.9         # UploadFile 파일 업로드 처리 (필수)

# ── 보안 스캐너 ────────────────────────────────────────────
checkov>=3.2.0                  # Terraform IaC 스캐너

# ── DB (스캔 이력 저장, Day 3~4) ──────────────────────────
sqlalchemy>=2.0.0               # SQLite ORM

# ── 데이터 전처리 ────────────────────────────────────────────
pandas